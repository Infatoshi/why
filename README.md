```bash
git clone https://github.com/infatoshi/why.git
cd why
brew install node && node -v && npm -v
npm install -g markmap-cli
markmap life.mm.md -o life.html
# drag and drop life.html in browser
```